---
---

<section>
  <div></div>
  <form>
    <input type="text" name="firstname" required placeholder="Fornavn*" />
    <input type="text" name="lastname" required placeholder="Efternavn*" />
    <input type="text" name="mail" placeholder="minmail@gmail.com" />
    <input class="all" type="text" name="Overskrift" placeholder="Overskrift" />
  </form>
</section>
<style>
  section {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }

  input {
    font-family: gill-sans-nova, sans-serif;
    font-weight: 400;
    font-style: normal;
    font-size: 1rem;
    background-color: none;
    border: 0.1px solid var(--primarycolor);
    padding: 0.5rem 1rem 0.5rem 1rem;
    color: black;
  }

  input::placeholder {
    color: black;
    opacity: 0.7; /* Firefox */
  }

  form {
    margin-left: 1rem;
    margin-right: 1rem;
    margin-bottom: 1rem;
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 0.5rem;
  }
  .all {
    grid-area: span1/3;
  }
</style>
